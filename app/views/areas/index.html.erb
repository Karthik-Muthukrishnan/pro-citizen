<% provide(:title, 'All circles') %>
<div class="row">
<aside class="span4">
	<section>
        <%= render 'shared/user_info' %>
    </section>
    <%= render "shared/user_side_panel" %>
  </aside>
  <div class="span8">
	<h1>All circles</h1>

<ul class="areas">
  <%= render partial: 'area', collection: @areas_all %>
</ul>
<%= will_paginate @areas_all%>

</div>
</div>